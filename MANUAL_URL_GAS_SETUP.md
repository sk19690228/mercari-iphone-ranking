# 型落ちiPhoneおすすめランキング用・新規GAS設定手順

このアプリ専用の新しいApps Scriptプロジェクトを作成します。既存のv41用GASは変更しません。

## 1. 新規プロジェクトを作成

1. Apps Scriptで「新しいプロジェクト」を作成します。
2. プロジェクト名を「メルカリiPhoneランキングGAS」にします。
3. 既定の `Code.gs` を削除し、このリポジトリの `Code.gs` を貼り付けて保存します。
4. 「プロジェクトの設定」→「スクリプト プロパティ」を開きます。
5. `APP_TOKEN` に推測されにくい長い文字列を設定します。

Gmail権限の承認は不要です。

## 2. 新しいウェブアプリとしてデプロイ

1. 「デプロイ」→「新しいデプロイ」を開きます。
2. 種類は「ウェブアプリ」を選択します。
3. 「次のユーザーとして実行」は「自分」を選びます。
4. 「アクセスできるユーザー」は「全員」を選びます。
5. デプロイし、末尾が `/exec` のURLをコピーします。

## 3. 新アプリへ接続

1. 新アプリの「自動検索・選定条件」を開きます。
2. 新しいGASウェブアプリURLを入力します。
3. GASの `APP_TOKEN` と同じ中継用トークンを入力します。
4. 「URL取得用GASをテスト」を押します。
5. `メルカリURL取得中継 vranking-1` と表示されれば接続完了です。

アプリを開き直すと新着検索が始まり、割引率順の上位3件がランキングへ反映されます。
